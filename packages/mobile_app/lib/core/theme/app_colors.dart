import 'package:flutter/material.dart';

class AppColors {
  // Color principal de la aplicación (Verde corporativo)
  static const primary = Color(0xFF005346);

  // Colores fijos para Accesos Directos (Dashboard)
  static const cotizar = Colors.indigo;
  static const clientes = Colors.blue;
  static const deudas = Colors.red;
  static const reportes = Colors.teal;
  static const kardex = Color(0xFF4F7091);

  // Colores para Accesos Administrativos
  static const proveedores = Colors.purple;
  static const personal = Colors.green;
  static const almacenes = Colors.deepOrange;
  static const empresa = Colors.orange;
}
