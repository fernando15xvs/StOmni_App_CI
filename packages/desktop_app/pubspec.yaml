name: desktop_app
description: "StOmni desktop client."
publish_to: 'none'
version: 1.0.0+1

resolution: workspace

environment:
  sdk: ^3.10.8

dependencies:
  core_logic:
    workspace: ^
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  supabase_flutter: ^2.5.0
  uuid: ^4.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
